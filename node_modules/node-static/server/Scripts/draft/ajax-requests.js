var
  offers = [],
  user = {
    fullname: 'Гость',
    avatar: '../../../images/users/anonim.png'
  };

$('.login .button').on('click', function () {
  var id = $('.login .field-text').val();

  if (id) {
    getLoggedUser(id)
  }
})

getOffers();

function getOffers() {
  $.ajax({
    type: 'GET',
    url: 'offers.json',
    success: function (data) {
      $.each(data, function () {
        offers.push(this);
      });
      renderShortOffers();
    }
  });
}

function offerRefresh() {
  $.ajax({
    type: 'POST',
    data: JSON.stringify(offers),
    url: '/refresh',
    contentType: 'application/json',
    success: function (data) {
      try {
        offers = JSON.parse(data).slice();
      } catch (e) {
        offerRefresh();
      }
    }
  });
}

function getLoggedUser(id) {

  $.ajax({
    type: 'POST',
    data: id,
    url: '/getUser',
    contentType: 'application/json',
    success: function (data) {
      user = JSON.parse(data);
      renderLoggedUser();
      renderShortOffers();
    }
  })
}